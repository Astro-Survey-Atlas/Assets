# Connector 清理与 Euclid Q1 MER 数据用途

2026-09-21 盘点记录；产品边界于 2026-09-23 澄清。核心目标是从实际数据建立覆盖，
按重合区域定位来源与文件，让用户知道哪里有数据、依据是什么、可以去哪里获取。

## 删除连接

后台“数据来源”选中连接后，在详情操作区点击“删除连接”，确认后执行。
只删除 Assets 管理的连接 ConfigMap 及探测/盘点缓存；不删除桶、对象、PVC、扫描历史、
覆盖索引或已发布产品。任务状态读取失败或关联扫描未结束时拒绝删除。
Warehouse 原生 AstroDataSource 需在其来源系统管理，本页按钮禁用。

保留 Secret，因为历史冻结 ScanPlan 或其他连接可能引用它；删除连接不等于撤销
存储凭证。新建同名连接使用新 Secret，不覆盖历史凭证。新扫描/通过 Assets 重试前
需要重新建立连接。界面不自动挑选或批量删除任何过时连接。

## euclid-q1-mer-catalog 的只读核实

- 当次盘点确认配置指向 Q1 的整个 MER 前缀，并非纯星表路径；下面数量是当时的
  目录盘点，不是持续更新的科学文件索引。后续单 Tile 扫描及批次进度见 `HANDOFF.md`。
- 分页列举完成，无截断，共 17,597 个对象条目：1,649 个目录占位，15,948 个文件。
- 按文件名分类：BGSUB-MOSAIC-VIS 352 个；BGSUB-MOSAIC-NIR-H/J/Y 各 352 个。
  还有地面辅助波段、BGMOD、PSF、RMS 与 FLAG，不能混作同一科学产品。
- 未找到普通源星表命名的对象，不能仅凭 connector 名称认定为星表。
  抽查 CATALOG-PSF-DES-G 的扩展是 IMAGE / PSF image，并非 RA/Dec 源表。
- 抽查一个 VIS BGSUB-MOSAIC FITS 前 128 KiB：19200×19200，RADESYS=ICRS，
  RA---TAN / DEC--TAN，CRVAL/CRPIX/CD 齐全，可作为 WCS 覆盖计算输入。
  未读取所有文件头，未证明全部文件完整、有效或代表整个官方 Q1。
- 本次仅列表与限量 Range 读取，没有创建扫描、构建或发布任务，没有改变连接。

## 围绕覆盖和反查的接入顺序

1. 先限定一个 Tile 的 VIS 科学影像验证；按仪器、波段和产品类型筛选，排除背景/PSF。
2. 以 FITS WCS 生成文件边界，保留 Tile ID、源对象身份、大小、坐标系和可用下载入口。
   边界和有效像素掩膜不同；有空白/坏像素时应披露限制，不能宣称每个像素有效。
3. 建立空间像元到原文件的覆盖索引，验证真实重合区域只返回相交 Tile 文件。
4. 扩展到 VIS 与 NISP H/J/Y；RMS/FLAG 按需关联到对应科学影像。
5. 构建可审核的 MOC/索引并按原有门禁发布，明确镜像已扫描范围与完整公开 MOC 的区别。

输出是可浏览、可导出的下载计划文件，包含 Tile/文件身份、匹配依据、来源和可用链接，
不包含科学数据。Assets 绝不替用户下载科学数据，也不承担 FITS 裁切或星表行提取服务。
用户在来源系统自行获取数据；私有存储位置按原始 URI 展示，不能冒充公开下载地址。
没有链接时仍展示真实文件名、Tile、来源及已知限制，不将缺少直链视为接入失败。

Warehouse 的 `catalog-radec` 已在本地补充 FITS BINTABLE 解析：明确选择 `hduName` 或
零基 `hduIndex`，声明 `coordinateFrame=ICRS` 和坐标列，按真实目标坐标建立索引。
部署与验收状态见 `HANDOFF.md`，不能仅凭源码已实现就假设线上可用。PSF 影像及表头
中心点仍不属于源星表分布。
